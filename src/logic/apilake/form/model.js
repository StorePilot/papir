/**
 * Model
 */
export default class Model {

  constructor (map) {

    // If map === object and type, resolve map

    // else, use map as url

    
    // This should be based on tech from old_modelling_system and new auth / form system
  }

}